package Resources;

import Processes.ProcessDescriptor;

public class DynamicResource extends Resource {
    public DynamicResource(ProcessDescriptor creator, String userName) {
        super(creator, userName);
    }
}
