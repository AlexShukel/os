package Resources;

import Processes.ProcessDescriptor;

public class StaticResource extends Resource {
    public StaticResource(ProcessDescriptor creator, String userName) {
        super(creator, userName);
    }
}
